# awbot/envs/dpy/.env.example
# Environment configuration for the DPY (discord.py) bot

############################################################
# This file references the global template at:
#   awbot/envs/.env.example
#
# To set up your environment:
#   1. Copy awbot/envs/.env.example to awbot/envs/dpy/.env
#   2. Override any values below that are specific to DPY.
#   3. Do NOT commit secrets to version control!
############################################################

# Example overrides for DPY bot (uncomment and set as needed):

# DPY_PREFIX=!
# DPY_OWNER_ID=your-owner-id

# You can also override any global variable here, e.g.:
# DISCORD_TOKEN=your-dpy-discord-token
# LOG_LEVEL=debug

# For all available variables, see awbot/envs/.env.example